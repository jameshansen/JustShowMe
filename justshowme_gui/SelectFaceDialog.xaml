<Window x:Class="JustShowMe.SelectFaceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Face"
        Height="400" Width="500"
        WindowStartupLocation="CenterOwner"
        Background="#2B2B2B">

    <Border BorderBrush="Cyan" BorderThickness="2" Margin="10">
        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Select Face" FontSize="16" FontWeight="Bold"
                       Foreground="White" Margin="0,0,0,15"/>

            <ListBox Grid.Row="1" Name="FaceListBox" Background="#3C3C3C" BorderBrush="Gray">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="10">
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14" Foreground="White"/>
                                <TextBlock Text="{Binding StatusText}" FontSize="12" Foreground="LightGray" Margin="10,2,0,0"/>
                            </StackPanel>
                            <!-- Up to 5 latest snapshots grouped for this one identity. -->
                            <ItemsControl ItemsSource="{Binding Snapshots}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate><StackPanel Orientation="Horizontal"/></ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Image Width="56" Height="56" Source="{Binding Image}" Margin="0,0,6,0"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                <Button Content="Cancel" Width="80" Margin="0,0,10,0" Click="Cancel_Click"/>
                <Button Content="Next" Width="80" Click="Next_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
